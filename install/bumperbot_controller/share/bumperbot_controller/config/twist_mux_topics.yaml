twist_mux:
  ros__parameters:
    topics:
      joystick:
        topic   : joy_vel
        timeout : 0.5
        priority: 99
      keyboard: 
        topic   : key_vel
        timeout : 0.5
        priority: 90
      navigation:
        topic   : /cmd_vel
        timeout : 0.5
        priority: 100  # Higher than joystick and keyboard
